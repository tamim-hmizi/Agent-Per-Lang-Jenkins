pipeline {
    agent {
        label 'dockeragent' 
    }
    stages {
        stage('Checkout') {
            steps {
                git branch: 'JS', url: 'https://github.com/tamim-hmizi/Agent-Per-Lang-Jenkins.git'
            }
        }
        stage('Running index.js') {
            steps {
                sh 'node index.js'
            }
        }
    }
}
